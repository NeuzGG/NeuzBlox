# ============================================================
#  NEVER COMMIT THE PRIVATE LICENCE KEY.
#  Anyone who gets this file can mint unlimited NeuzBlox keys,
#  and you cannot revoke them without invalidating every key
#  you have already issued.
# ============================================================
tools/keys/
*.blob

# ============================================================
#  SOURCE IS NOT PUBLISHED.
#  NeuzBlox is a paid tool, so the public repo carries only the
#  built exe, the site and the README.
#
#  These folders exist ONLY on your machine. Git will not save
#  them and GitHub will never have a copy — so keep your own
#  backup somewhere else, or losing this PC loses the project.
# ============================================================
src/
tools/
build/
build.cmd

# local scratch
*.user
*.suo
.vs/
